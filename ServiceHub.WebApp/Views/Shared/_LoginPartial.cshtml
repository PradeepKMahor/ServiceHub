@using Microsoft.AspNetCore.Identity
@using ServiceHub.Domain
@inject SignInManager<AppUser> SignInManager
@inject UserManager<AppUser> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item form-inline">
            <a class="nav-link btn btn-link  pr-0 text-white" asp-area="Identity" asp-page="/Account/Manage/Index"
               title="Manage">
                <i class="fa fa-user  text-white" aria-hidden="true"></i>
                Hello @User.Identity?.Name! &nbsp;
            </a>
            <form class="form-inline " asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" class="nav-link btn btn-link  pl-0 text-white">
                    <i class="fas fa-sign-out-alt text-white"></i>
                    Logout
                </button>
            </form>
        </li>
    }
    else
    {
        @*  <li class="nav-item">

    <a asp-area="Masters"
    asp-controller="UsersClient"
    asp-action="CreateClientUserIndex"
    class="nav-link  text-white">
    <i class=" fas fa-user text-white" aria-hidden="true"></i>&nbsp;
    Register
    </a>
    </li> *@
        <li class="nav-item">
            <a class="nav-link  text-white" asp-area="Identity" asp-page="/Account/Login">
                <i class=" fas fa-user text-white" aria-hidden="true"></i>&nbsp; Login
            </a>
        </li>
    }
</ul>
